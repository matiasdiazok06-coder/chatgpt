# Valores por defecto para los parámetros de envío y auto-respuesta
# Podés crear un `.env.local` para sobrescribirlos sin tocar este archivo.
MAX_PER_ACCOUNT=10
MAX_CONCURRENT=2
DELAY_MIN=5
DELAY_MAX=15
AUTORESPONDER_DELAY=10
