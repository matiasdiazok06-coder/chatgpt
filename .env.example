# Valores por defecto para la app. Copiá este archivo a `.env` y ajustá,
# o creá `.env.local` para overrides que no quieras commitear.

# Control de cuentas y envío
MAX_PER_ACCOUNT=10       # 2-50 mensajes por cuenta
MAX_CONCURRENCY=5        # 1-20 cuentas en simultáneo
DELAY_MIN=45             # >=10 segundos
DELAY_MAX=55             # >= DELAY_MIN
AUTORESPONDER_DELAY=10   # segundos entre chequeos del bot

# Flags de experiencia
QUIET=0                  # 1 = modo silencioso (solo resúmenes)
LOG_DIR=storage/logs
LOG_FILE=app.log

# Integraciones
OPENAI_API_KEY=
SUPABASE_URL=
SUPABASE_KEY=
